# ToolBelt — public site

Marketing site for ToolBelt, an on-demand marketplace connecting customers with
vetted local workers for home and property jobs.

Three static pages, no build step, no dependencies. Served via GitHub Pages.
Source of truth is `web/` in the (private) main application repository — edit
there and copy across.
